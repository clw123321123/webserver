#include "../kong/Kong.h"

static kong::Logger::ptr g_logger = KONG_LOG_NAME("test");

int main() {
    // kong::HttpResponse::ptr httpresponse(new kong::HttpResponse);
    // httpresponse->Init("hey", "kong", true, );

    return 0;
}